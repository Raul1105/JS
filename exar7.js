// Soma de numeros do array

let lista = [32,5,89,45,26];
let soma = 0;

for (let i = 0; i < lista.length; i++){
    soma += lista[i];
}

console.log(`A soma de todos os numeros do array é de ${soma}`)