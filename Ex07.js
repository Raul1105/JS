/* Verificação de idade */

let idade = 16;

if (idade >= 18){
    console.log(`Com a idade de ${idade} você pode entrar na balada!`);
} else {
    console.log(`Com a idade de ${idade} você não pode entrar na balada!`);
}