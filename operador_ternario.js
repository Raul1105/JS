/*Usado com condicional simples sendo o primeiro como condição de sim e o segundo condição de não */

let chuva = false

let guarda_chuva = chuva ? 'Levar guarda-chuva' : 'Não levar guarda-chuva';

console.log(guarda_chuva)